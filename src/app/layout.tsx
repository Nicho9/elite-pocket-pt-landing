import type { Metadata } from "next";
import { Analytics } from "@vercel/analytics/react";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Elite Pocket PT | Personalised Training, Nutrition & Mobility Coaching",
  description:
    "Elite Pocket PT is a science-backed coaching system built by Coach Mike, combining personalised training, nutrition, mobility, progress tracking, and daily coaching support.",
  verification: {
    google: "t6OdLrDo6s-RYwQ6ouYcO2BQpSc-ixYyzZQLuNIGM2Q",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">
        {children}
        <Analytics />
      </body>
    </html>
  );
}
